import { SystemAdminShell } from "@/components/SystemAdminShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { Activity, AlertTriangle, CheckCircle2, CircleAlert, Database, HardDrive, Loader2, RefreshCw, Sparkles } from "lucide-react";

const icons = { mysql: Database, storage: HardDrive, ai: Sparkles };

export default function SystemDashboard() {
  const health = trpc.system.health.useQuery(undefined, { refetchInterval: 30_000, retry: false });
  const problemCount = health.data?.filter(item => item.status !== "healthy").length ?? 0;
  return <SystemAdminShell><section className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="mb-2 flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.16em] text-[#53766a]"><span className="size-1.5 rounded-full bg-[#a5d83d]" /> Live infrastructure</p><h1 className="font-display text-3xl font-semibold tracking-[-.055em] sm:text-[40px]">System <span className="text-[#789c55]">dashboard.</span></h1><p className="mt-2 max-w-xl text-sm leading-6 text-[#67726e]">ตรวจการเชื่อมต่อสำคัญของ Cineflow ทุก 30 วินาที พร้อมรายละเอียดที่ใช้แก้ปัญหาได้ โดยไม่แสดง credentials</p></div><Button variant="outline" onClick={() => void health.refetch()} disabled={health.isFetching} className="border-[#ceddc4] bg-white text-[#486643] hover:bg-[#edf5e5]">{health.isFetching ? <Loader2 className="animate-spin" /> : <RefreshCw />} Refresh now</Button></section>{problemCount > 0 && <section className="mt-6 flex gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-amber-900"><CircleAlert className="mt-0.5 shrink-0" size={18} /><div><p className="text-sm font-bold">ต้องตรวจสอบ {problemCount} บริการ</p><p className="mt-1 text-xs leading-5">การแจ้งเตือนนี้จะแสดงจนกว่าการตรวจครั้งถัดไปจะสำเร็จ ดูรายละเอียดของแต่ละบริการด้านล่าง</p></div></section>}{health.isLoading ? <div className="mt-7 grid min-h-48 place-items-center rounded-3xl border border-[#dce5d5] bg-white"><Loader2 className="animate-spin text-[#6c963f]" /></div> : <section className="mt-7 grid gap-4 md:grid-cols-3">{health.data?.map(item => { const Icon = icons[item.id]; const healthy = item.status === "healthy"; return <article key={item.id} className="rounded-[24px] border border-[#dce5d5] bg-white p-5 shadow-[0_12px_36px_rgba(31,43,37,.04)]"><div className="flex items-start justify-between gap-3"><div className={`grid size-10 place-items-center rounded-xl ${healthy ? "bg-[#edf6de] text-[#5f8c35]" : item.status === "unconfigured" ? "bg-stone-100 text-stone-600" : "bg-rose-50 text-rose-700"}`}><Icon size={19} /></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide ${healthy ? "bg-emerald-50 text-emerald-700" : item.status === "unconfigured" ? "bg-stone-100 text-stone-600" : "bg-rose-50 text-rose-700"}`}>{item.status}</span></div><h2 className="mt-5 font-display text-lg font-semibold">{item.label}</h2><p className="mt-2 min-h-10 text-xs leading-5 text-[#68766f]">{item.detail}</p><div className="mt-5 flex items-center gap-1.5 border-t border-[#edf0ea] pt-3 text-[10px] text-[#859089]">{healthy ? <CheckCircle2 size={12} className="text-emerald-600" /> : <AlertTriangle size={12} className="text-amber-600" />} Checked {new Date(item.checkedAt).toLocaleTimeString()}</div></article>; })}</section>}<section className="mt-5 rounded-[24px] border border-[#dce5d5] bg-[#1b2421] p-5 text-white"><div className="flex items-start gap-3"><div className="grid size-9 place-items-center rounded-xl bg-[#2c3c36] text-[#c5f165]"><Activity size={17} /></div><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#c4d0c8]">Operational guidance</p><p className="mt-1 text-sm leading-6 text-[#e7eee8]">หากสถานะเป็น degraded ให้ตรวจ endpoint, key และ network ของ provider ใน Settings; งานใหม่จะใช้ configuration ที่บันทึกไว้ทันทีโดยไม่ต้อง restart container.</p></div></div></section></SystemAdminShell>;
}
